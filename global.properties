# Global framework configuration
environment=QA
browser=chrome
headless=false
implicit.wait.seconds=10
explicit.wait.seconds=30
page.load.timeout.seconds=60
screenshot.on.failure=true
rerun.failed.scenarios=false
